{
  "name": "users-service-example",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript HTTP API (payload v2) with a local simple-response authorizer; createUser publishes UserSignedUp to the shared domain-events bus. LSS runs the .ts handlers straight from source via esbuild-register — no build step. Port 3610 (invoke 13610).",
  "scripts": {
    "package": "serverless package",
    "register": "serverless package"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "dotenv-cli": "^7.4.4",
    "esbuild": "^0.28.1",
    "esbuild-register": "^3.6.0",
    "osls": "4.0.0",
    "serverless-lss": "file:../../../packages/serverless-plugin",
    "typescript": "^5.0.0"
  }
}
