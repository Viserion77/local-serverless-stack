# Copy to `.env` (which is gitignored) and fill in.
#
# LocalStack community images >= 2026.5 prompt for a token even when running in
# free mode. Provide one to silence the warning and unlock the latest images.
# Get a free token at https://app.localstack.cloud (Personal access tokens).
LOCALSTACK_AUTH_TOKEN=
