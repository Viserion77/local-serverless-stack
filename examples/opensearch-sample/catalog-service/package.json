{
  "name": "catalog-service-example",
  "version": "0.0.0",
  "private": true,
  "description": "POST /products indexes into OpenSearch; GET /search runs full-text + filtered queries; GET /stats aggregates. Port 3641 (invoke 13641). No runtime dependencies — the handlers speak the OpenSearch REST API with Node's global fetch.",
  "scripts": {
    "package": "serverless package",
    "register": "serverless package"
  },
  "devDependencies": {
    "serverless": "4.39.0",
    "serverless-lss": "file:../../../packages/serverless-plugin"
  }
}
