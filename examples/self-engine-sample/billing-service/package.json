{
  "name": "billing-service-example",
  "version": "0.0.0",
  "private": true,
  "description": "SQS consumer that writes a receipt to S3 and publishes OrderBilled to the billing-events bus; GET /receipts lists receipts. Port 3032 (invoke 13032).",
  "scripts": {
    "package": "serverless package",
    "register": "serverless package"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0"
  },
  "devDependencies": {
    "serverless": "3.40.0",
    "serverless-lss": "file:../../../packages/serverless-plugin"
  }
}
