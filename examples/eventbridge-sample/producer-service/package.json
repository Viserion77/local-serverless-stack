{
  "name": "producer-service-example",
  "version": "0.0.0",
  "private": true,
  "description": "HTTP API (payload v2) that publishes UserSignedUp events to the shared domain-events bus with PutEvents. Port 3621 (invoke 13621).",
  "scripts": {
    "package": "serverless package",
    "register": "serverless package"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.0.0"
  },
  "devDependencies": {
    "serverless": "3.40.0",
    "serverless-lss": "file:../../../packages/serverless-plugin"
  }
}
