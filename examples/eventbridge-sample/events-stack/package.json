{
  "name": "events-stack-example",
  "version": "0.0.0",
  "private": true,
  "description": "Resources-only stack owning the shared EventBridge bus (domain-events). No functions, no ports — registering it just provisions the bus in LocalStack.",
  "scripts": {
    "package": "serverless package",
    "register": "serverless package"
  },
  "devDependencies": {
    "serverless": "3.40.0",
    "serverless-lss": "file:../../../packages/serverless-plugin"
  }
}
