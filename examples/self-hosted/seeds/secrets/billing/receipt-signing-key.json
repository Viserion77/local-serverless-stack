{
  "description": "HMAC key for signing billing receipts — seeded on boot so the very first GET /receipts/{id}/signature finds an AWSCURRENT version (no lazy CreateSecret bootstrap needed).",
  "secretString": "dev-receipt-signing-key-please-rotate"
}
