{
  "name": "orders-service-example",
  "version": "0.0.0",
  "private": true,
  "description": "JavaScript REST API (payload v1) guarded by auth-service's authorizer (cross-service, by ARN), with an SQS consumer and a scheduled cleanup Lambda. Port 3612 (invoke 13612).",
  "scripts": {
    "package": "serverless package",
    "register": "serverless package"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-sqs": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.4",
    "serverless": "3.40.0",
    "serverless-lss": "file:../../../packages/serverless-plugin"
  }
}
