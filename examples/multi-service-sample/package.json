{
  "name": "multi-service-sample",
  "version": "0.0.0",
  "private": true,
  "description": "Three microservices (REST v1 JS, HTTP API v2 TypeScript, cross-service authorizer + SQS) served entirely by LSS's API Gateway + Lambda runtime emulation — no serverless-offline.",
  "scripts": {
    "lss:start": "node ../../bin/cli.js start",
    "lss:stop": "node ../../bin/cli.js stop",
    "lss:status": "node ../../bin/cli.js status",
    "lss:seed": "node ../../bin/cli.js seed",
    "register:auth": "npm --prefix auth-service run package",
    "register:users": "npm --prefix users-service run package",
    "register:orders": "npm --prefix orders-service run package",
    "register:all": "npm run register:auth && npm run register:users && npm run register:orders",
    "setup": "npm --prefix auth-service install && npm --prefix users-service install && npm --prefix orders-service install"
  }
}
