// One source for every user-facing mention of the engine — header badge,
// Overview cards, empty states, delete confirmations. A named export rather
// than a literal sprinkled through templates, so renaming it is one edit.
export const ENGINE_LABEL = 'self engine';
